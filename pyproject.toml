[project]
name = "bas-vla"
version = "0.1.0"
description = "Open-source codebase for BAS-VLA."
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["bas_vla*"]
